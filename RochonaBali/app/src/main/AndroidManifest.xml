<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.destroy.rochonabali">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Rabindranath" />
        <activity android:name=".Nazrul" />
        <activity android:name=".SaratChandra" />
        <activity android:name=".Bonkim" />
        <activity android:name=".Show_bonkim" />
        <activity android:name=".Show_rabindranath" />
        <activity android:name=".Show_nazrul" />
        <activity android:name=".Show_sharat" />
        <activity android:name=".Choton"></activity>
    </application>

</manifest>